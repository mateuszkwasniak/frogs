type FrogT = {
  id: number;
  name: string;
  gender: "male" | "female";
  height: "tall" | "short";
  figure: "fat" | "slim";
  position: string;
};

type BoardCoordinate = [number, number];

type BoardCoordinateKey = string;

type BoardValue = number | null;
